1. 单环境能力测试：
 a.测试最多可以支持的trans数：
   CHAIN_NU_TOTAL = {"R": 1, "S": 1}  # 固定
   CHAIN_NU_LOCAL = {"R": 1, "S": 1}  # 固定
   SS_TRADE_NU = 600  # 测试修改 ss_trade_number , 从较大的数值开始测试, 没问题则增加 R|S 值，逐步测试
   STRESS_LOOP_TIME = 0  # 设置为0, 一直运行
   STRESS_LOOP_INTERVAL = 15  # 设置每轮B->R->S每轮发送最大间隔15秒

 b.测试最多可以支持的R|S数：
    CHAIN_NU_TOTAL = {"R": 32, "S": 1024}   # 测试修改 R|S, 从较大的数值开始测试
    CHAIN_NU_LOCAL = {"R": 32, "S": 1024}   # 测试修改 R|S, 从较大的数值开始测试， 没问题则增加 ss_trade_number 的值，逐步测试
    SS_TRADE_NU = 1  # 固定
    STRESS_LOOP_TIME = 0  # 设置为0, 一直运行
    STRESS_LOOP_INTERVAL = 15  # 设置每轮B->R->S每轮发送最大间隔15秒

